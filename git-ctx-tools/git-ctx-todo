#!/usr/bin/env bash

# git-ctx-todo: Route todo sub-commands
HELPER_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &> /dev/null && pwd)"
source "$HELPER_DIR/git-ctx-helper.sh"

TODO_CMD="${1:-list}"
shift || true

case "$TODO_CMD" in
    list)
        "$HELPER_DIR/git-ctx-show" "$@"
        ;;
    add)
        "$HELPER_DIR/git-ctx-add" "$@"
        ;;
    done|check)
        "$HELPER_DIR/git-ctx-done" "$@"
        ;;
    undo|uncheck)
        "$HELPER_DIR/git-ctx-undo" "$@"
        ;;
    rm|delete)
        "$HELPER_DIR/git-ctx-rm" "$@"
        ;;
    clear)
        "$HELPER_DIR/git-ctx-clear" "$@"
        ;;
    *)
        echo -e "${RED}Unknown todo sub-command: $TODO_CMD${NC}"
        echo "Run 'git ctx help' for usage."
        exit 1
        ;;
esac
